export const metadata = {
  title: "Explore Learning Paths",
  description:
    "Choose your path and start learning practical, in-demand skills — all for free. ",
};

export default function Layout({ children }) {
  return <div>{children}</div>;
}
