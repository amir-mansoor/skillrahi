// import { auth } from "@/auth";
// import { redirect } from "next/navigation";

// export async function protectedRoute() {
//   const session = await auth();

//   if (!session?.user) {
//     redirect("/auth");
//   }

//   return session; // useful if we need user info in page
// }
